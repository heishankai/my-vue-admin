# 仅 development：`pnpm dev` / `vite`（默认 mode=development）
# 需暴露到前端源码中的变量必须以 VITE_ 开头
VITE_APP_TITLE=Vue Admin (dev)
# 开发环境必须留空（或 /），让请求走当前页同源 http://localhost:5173/api/**，才会命中 vite.config 里的 proxy，避免浏览器直连 ceshi13 触发 CORS
VITE_API_BASE_URL=
